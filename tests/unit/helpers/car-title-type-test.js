
import { carTitleType } from 'car-dealership/helpers/car-title-type';
import { module, test } from 'qunit';

module('Unit | Helper | car title type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = carTitleType([42]);
  assert.ok(result);
});

